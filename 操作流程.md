# SuperPoint + LightGlue 無 GPS 定位系統 — 完整操作流程

## 系統概覽

```
步驟 1：下載衛星圖資 (Tile)
    ↓
步驟 2：建立特徵索引 (Build Index)
    ↓
步驟 3：無人機拍照取得空拍影像
    ↓
步驟 4：執行定位比對 (Localize)
    ↓
輸出：估算 GPS 座標
```

---

## 步驟 1：下載衛星圖資

**目的**：從 ESRI 衛星影像伺服器下載指定區域的地圖 Tile，儲存到本地端。

**相關檔案**：`../gpsless_mapping/download_tiles.py`

**設定飛行區域**（修改 `config.py`）：
```python
TAIPEI_BBOX = {
    'lat_min': 25.02,
    'lat_max': 25.09,
    'lon_min': 121.43,
    'lon_max': 121.51,
}
ZOOM_LEVEL = 19   # zoom 19 ≈ 0.3 m/pixel，適合無人機定位
```

**執行命令**（在 `gpsless_mapping/` 目錄下執行）：
```bash
cd /home/scott/claude-project/gpsless_mapping
python3 download_tiles.py
```

或指定小範圍測試：
```bash
python3 download_tiles.py \
    --lat-min 25.04 --lat-max 25.06 \
    --lon-min 121.47 --lon-max 121.49 \
    --zoom 19
```

**下載後的目錄結構**：
```
gpsless_mapping/tiles/
└── 19/
    └── {tile_x}/
        └── {tile_y}.png
```

**Tile 伺服器**：
```
ESRI World Imagery（免費，無需 API Key）
URL: https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}
注意：URL 順序是 z/y/x，不是 z/x/y
```

---

## 步驟 2：建立特徵索引

**目的**：對每張 Tile 用 SuperPoint 提取特徵點（keypoints）和描述子（descriptors），儲存成索引檔，供後續快速比對。

**重要**：索引和查詢必須使用相同的縮放設定（`resize=640`），否則描述子空間不一致，定位會失敗。

**執行命令**（在 `gpsless_superpoint/` 目錄下執行）：
```bash
cd /home/scott/claude-project/gpsless_superpoint
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64 python3 build_index.py
```

**執行結果**（成功範例）：
```
Device        : cuda
Tiles found   : 660 (zoom 19)
Max kp/tile   : 512
...
Index saved → index/sp_index_z19.pkl
  Tiles indexed  : 660
  Total keypoints: 159,914
  Index file size: 165.7 MB
```

**何時需要重建索引**：
- 第一次使用，尚無索引檔
- 下載了新的 Tile（擴大飛行範圍）
- 修改了 `SP_MAX_KEYPOINTS`、`SP_KEYPOINT_THRESHOLD` 等參數
- 修改了 `ZOOM_LEVEL`

> ⚠️ **注意**：`build_index.py` 內部已設定 `extractor.preprocess_conf = {'resize': 640}`，
> 若修改此值，`localize.py` 裡的同一設定也必須同步修改，然後重建索引。

---

## 步驟 3：無人機拍照

**目的**：取得無人機對地垂直拍攝的影像，作為定位查詢的輸入。

**拍攝注意事項**：

| 項目 | 建議值 | 說明 |
|------|--------|------|
| 飛行高度 | 60–100 m | 太低視角偏差大；太高解析度不足 |
| 相機角度 | 接近垂直（nadir） | roll/pitch 超過 5° 定位準確度會大幅下降 |
| 地面紋理 | 豐富（建築、跑道、籃球場） | 草地、停車場等低紋理區域容易 NO FIX |
| 影像格式 | PNG 或 JPG | 建議 1280×720 以上 |

**Attitude Guard**：`drone_localize.cpp` 已設定當 roll 或 pitch 超過 5° 時自動跳過拍照：
```cpp
const float MAX_TILT_DEG = 5.0f;
```

---

## 步驟 4：執行定位比對

**目的**：輸入無人機拍攝的影像，與索引中的衛星 Tile 比對，輸出估算的 GPS 座標。

### 4-1 單張照片定位

```bash
cd /home/scott/claude-project/gpsless_superpoint
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64 python3 localize.py 照片路徑.png
```

**範例**：
```bash
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64 python3 localize.py \
    "photo_20260705/original/25.0434821_121.4747908-NOFIX_NOFIX__NAm__60.1m.png"
```

加上 `--show` 可輸出視覺化比對結果（`result_tile.png`、`result_query.png`）：
```bash
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64 python3 localize.py 照片.png --show
```

### 4-2 成功輸出範例

```
=======================================================
  GPS Location : 25.0433653,  121.4746477
  RANSAC inliers: 44  |  centre tile (439054,224451) z19
  Google Maps  : https://maps.google.com/?q=25.0433653,121.4746477
=======================================================
```

### 4-3 定位失敗範例（NO FIX）

```
Could not determine location.
Suggestions:
  • Check that the photo is within the downloaded tile area
  • Try a nadir (straight-down) photo with clear ground detail
  • Lower SP_KEYPOINT_THRESHOLD in config.py to detect more kps
```

---

## 定位演算法流程說明

```
輸入：無人機空拍影像 (1280×720 PNG)
    │
    ▼
[前處理] _img_to_tensor()
    將影像縮放至最長邊 640px（避免 Jetson NvMap 記憶體崩潰）
    │
    ▼
[Phase 1 — FLANN 粗定位]
    SuperPoint 提取查詢影像特徵（最多 1024 個 keypoints）
    與索引中所有 Tile 的描述子做最近鄰投票（top-1 NN）
    選出票數最高的前 20 個候選 Tile
    │
    ▼
[Phase 2 — LightGlue 精定位]
    對每個候選 Tile，取其 3×3 鄰居拼成 768×768 composite 圖
    SuperPoint 提取 composite 特徵（最多 2048 個 keypoints）
    LightGlue 神經網路匹配查詢影像與 composite 的特徵對
    RANSAC 計算 Homography（需 ≥ 12 個 inliers，且 inlier ratio ≥ 30%）
    將查詢影像中心點投影到 composite 座標 → 換算 GPS
    │
    ▼
輸出：lat, lon（WGS-84）
```

---

## 程式檔案對照表

| 檔案 | 用途 |
|------|------|
| `config.py` | 所有參數設定（區域、zoom、keypoint 數量等） |
| `../gpsless_mapping/download_tiles.py` | 下載衛星 Tile |
| `build_index.py` | 建立 SuperPoint 特徵索引 |
| `localize.py` | 單張照片定位（命令列） |
| `tile_utils.py` | Tile 座標 / GPS 換算工具 |
| `index/sp_index_z19.pkl` | 索引檔（165.7 MB，660 tiles） |
| `../gpsless_mapping/tiles/19/` | 下載的衛星 Tile 圖資 |

---

## 常見問題排查

| 症狀 | 原因 | 解法 |
|------|------|------|
| `NvMapMemAllocInternalTagged error 12` | GPU 記憶體不足（NvMap 無法分配連續區塊） | 確認 `PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:64`；關閉其他佔用記憶體的應用程式 |
| Phase 1 只有 6–7 票 | 索引與查詢尺度不一致 | 確認 `build_index.py` 和 `localize.py` 兩處都有 `preprocess_conf = {'resize': 640}`；重建索引 |
| 定位誤差偏大 | 查詢中心點尺度錯誤 | 確認 `localize.py` 中 Homography 投影使用縮放後尺寸 `(qw/2, qh/2)` 而非原始尺寸 |
| 全部 NO FIX | 拍攝區域不在已下載的 Tile 範圍內 | 擴大 `TAIPEI_BBOX` 後重新下載 Tile 並重建索引 |
| GStreamer 卡住 | appsink 等待 clock sync | pipeline 加上 `appsink sync=false max-buffers=1 drop=true` |

---

## 目前定位效能（2026-07-05 飛行測試）

| 照片 | 真實 GPS | 估算誤差 | 備註 |
|------|----------|----------|------|
| `25.0434821_121.4747908` | lat 25.0434821, lon 121.4747908 | **19.4 m** | 跑道 / 籃球場區域 |
| `25.0431990_121.4743276` | lat 25.0431990, lon 121.4743276 | 127.9 m | 低紋理區域，匹配較弱 |
| 其餘 11 張 | — | NO FIX | 有蓋走廊、草地等低紋理地面 |
